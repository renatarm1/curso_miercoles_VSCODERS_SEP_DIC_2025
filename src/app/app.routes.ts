//contiene el mapa de rutas de tu aplicación
import { Routes } from '@angular/router';
import { Button } from './components/button/button'; 

export const routes: Routes = [
    { path: '', component: Button },
];
